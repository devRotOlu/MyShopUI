import React from "react";

import { activeOrdersProps } from "../types";

const ActiveOrders = ({ children }: activeOrdersProps) => {
  return <div>{children}</div>;
};

export default ActiveOrders;
